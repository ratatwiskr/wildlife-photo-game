{
  // Unique scene name — must match JPG and PNG base filenames.
  "name": "template_scene",

  // sceneType: "photo" (camera-based) or "wimmelbild" (direct-click search)
  "sceneType": "wimmelbild",

  // List of discoverable objects.
  "objects": [
    {
      // Unique per object. Prefer pattern: <type><index>
      "name": "animal1",

      // The exact color used in the mask PNG to mark this object.
      // Must be unique across all objects in the same scene.
      "color": "#ff0000",

      // Tags define logical categories or objective groups.
      // Example: ["object"], ["bird"], ["vehicle"], ["tree"]
      "tags": ["object"]
    }
  ],

  // Objectives define which tag groups must be found, in order.
  "objectives": [
    {
      // Displayed to adult or in icon form to player
      "title": "Find all objects 🦓",
      "tags": ["object"],
      "emoji": "🦓"
    }
  ]
}
